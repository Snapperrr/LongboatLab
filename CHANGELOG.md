# Longboat Lab 更新记录

以下记录对应各版本当时的实现，旧参数与规则可能已被后续版本替代。当前安装、依赖与玩法以 [README](README.md) 为准。最新模组版本为 0.10.19，随附最终地图以 V12 为基础。

## 0.10.19：船侧压力波重做与宽船材质平铺

- 重做连续高耸的船侧浪面：改用两端平滑回落、宽度约束高度的低压力波，船头起浪沿船身逐渐减弱。长船转弯按实际接触点向外推水的速度增强局部浪花，退水侧不获得同样增强。
- 白沫改为有清水间隙的破碎泡沫簇，沿较窄浪尖和外侧回落；浓白区域保留细泡壁和亮斑。泡沫量按局部浪高／宽度决定，避免直行长船从头到尾铺一条宽白膜。水滴从新的实际浪峰位置脱落，仍受重力作用。
- 水色层不再烘焙连续白亮边；修正各横向分片的 UV 偏移。侧浪与白沫使用保留低透明度的现有 Shader，减少边缘裁切。泡沫顶点由实际水浪网格插值得到，避免白色层穿入水面。
- 船侧、转弯和尾迹继续共用节点，新增破碎强度也随节点继承、插值和衰减；不使用每段突然开关泡沫的阈值。保持原有水面采样、射线和粒子池预算，浪面横向仍为 12 段并复用节点曲线缓存。
- 宽船改为按实际宽度烘焙船底、首尾板和横梁的几何及盒式 UV，新增表面从重复木纹图集中继续采样，保持每格 16 像素的横向纹理密度。左右侧壁平移并保持厚度；模型缓存以长宽共同索引，仍限制为 32 项，不按宽度新增分块。手持、物品栏与世界实体共用渲染修复，九种木材／竹材均适用。
- 完成源码语法、横截面回水、局部压力方向、浪高约束、泡沫覆盖和共用网格的离线检查，以及角度插值／水滴方向回归。核对 1.21.1 盒式 UV API、96 组宽度几何／材质密度及九种图集的重复性；附离线材质与几何预览工具。未执行 Gradle 编译或启动 Minecraft，游戏实际观感与帧率仍需实测。地图沿用 V12。

## 0.10.18：稳定后视、节点编辑器风格与气体／水花修整

- 新增默认 F7、可重绑的乘船稳定后视。进入时以玩家朝向为背面基准，随后只跟随船体中心平移，保持世界朝向和水平线；鼠标转头、船体自转／翻滚不再带转镜头。滚轮平滑调距 2.5～18 格，Shift 加视角键重新对齐。保留原版镜头避墙，此模式关闭喷气 FOV 冲击；F5 可切走，再次切换或下船恢复原视角，菜单滚轮不被拦截。
- F8 界面改为 Blender 节点编辑器式灰色画布、低饱和标题、橙色选中轮廓、命名端口和贝塞尔连线。新增节点拖动、折叠、侧栏开关及查看所选，沿用实时参数；缩短文案。该界面展示渲染流程，不修改 Shader 逻辑。
- 拍水移除放射状连续长水柱，改为不规则、弯曲折叠的短暂水冠；上缘先分段破碎，主体约半秒内消散，再由独立飞滴受重力下落。水膜和泡沫共用曲面缓存，脱落飞滴避免在出生刻重复推进。
- 船侧水滴重力增至 0.045 格/tick²，与薄雾分开；雾降低初始抬升、扩大薄片轮廓并缓慢扩散沉降。水滴池满时不再用长寿命雾粒替代水珠。
- 河豚喷气和水雾使用羽化纹理及独立透明 Shader，避免原版 alpha 0.1 裁剪将柔边切成硬轮廓。喷气沿流向拉伸、透明度按密度分摊，末段仍减速扩散。共享每个年龄段／船面的方向计算并复用旋转对象，减少多河豚渲染的重复计算和分配。
- 加宽、加厚船侧浪尖白沫，增加密集泡沫簇。白色层按浪面曲率细分，修正直线跨面穿入卷浪的情况；顶点 alpha 限定 0～255，避免加厚时溢出。
- 核实侧面河豚喷口与反冲方向、船重、乘员质量及世界向下重力；修复倾斜船只多个面朝上助推时重复叠加数量增益的问题。仅统一垂直合力，保持水平喷射和单面行为，足量河豚仍可克服重力。平移和翻滚使用同一实际推力。
- 版本更新为 0.10.18，客户端与服务端同步更新。完成 Java 语法、1.21.1 API、资源契约和离线数值检查；纹理／布局／曲面仅做离线预览。未执行 Gradle 编译或启动游戏，真实画面、视角操作、多人同步和帧率待实机验证。地图沿用 V12。

## 0.10.17：泡沫与立体飞溅、喷气热调、游戏内节点图

- 侧浪增加破碎白色浪尖和外侧薄沫，调整泡沫纹理的簇状密度；继续共用原有浪段端点、角度插值和尾迹轨迹，避免长船衔接重新产生断层。
- 拍水以短暂低水裙、封闭截面的弯曲收尖水柱和独立飞滴表现，减少整片扇面；细雾与水珠尺寸更小。保留 16 组拍水、384 水珠、192 细雾、48 涟漪的全局上限与既有查询预算。
- 修复侧面河豚向上放置跳过一行的问题：新放置从命中位置的下一行开始；旧存档的安装坐标和旧式列间距不被改写。
- 新增管理员 `/longboatlab jets density 0..8`、`force 0..16`、`reset` 和查询。默认均 1；世界持久化、在线广播、加入同步和断线重置。推力倍率同时影响实际加速度与力矩，重力仍独立计算。
- 新增可重绑 F8 的游戏内水面节点界面，12 个节点、拖拽／缩放／键盘选择、说明和实时计数。展示原版水体、压力、高度场、材质、侧尾浪、泡沫、立体飞溅、雾滴和最终合成；不是 Shader 编辑器。
- 核实现有多船高度场叠加：48×48 格区域、97×97 节点、3 子步、最多 8 船注入，装饰浪条独立演化。保留固定预算，没有增加逐船两两模拟。
- 地图 V12 将四处补给港池底抬至 Y=62、水面保持 Y=63，保留入口与服务台。导出 42 张实际存档方块正交图到 `Stage_Orthographs_V12`，另含带标题总览和坐标索引；更新最终地图目录与 ZIP，并将独立 V12 副本导入开发存档目录。
- Java 语法、河豚相邻行、立体截面、默认喷气参数、权限／存档／同步代码及既有浪花与接缝离线检查通过；V12 的 73,168 项地图几何检查通过。未执行 Gradle 编译或启动 Minecraft，实际水花效果、界面操作和多人热调仍需实机验证。

## 0.10.16：侧浪水雾流向与整圈转向插值

- **整圈转向抽搐**：原版网络插值会将角度归回 360 度以内，修正自定义显示角度跨越该边界时按普通数值插值、绕远路旋转的问题。世界中的船模型与乘员座位、相机、钩爪和交互选取统一使用最短角度插值；物品展示角度保持独立。
- **水雾流向**：去掉尾浪飞滴直接叠加反向航速的计算，改为继承各船舷接触点的部分运动速度并叠加水流和向外飞溅。水滴脱离后在世界坐标中飞行，相对行进船体逐渐落后；倒车和自转同样使用各处的真实运动方向。
- **连续飞溅**：沿整段浪尖分布细水滴和半透明轻雾，每 tick 统一给左右船舷与尾迹分配名额。出生帧跟随浪尖插值，轻雾逐渐减速、扩散和消失，寿命约 1.7～2.5 秒。保留原有水滴/轻雾池上限和世界采样预算，细喷雾的初速度封顶，避免高速船增加大量碰撞射线。
- **泡沫流动**：纹理轴固定在船体纵向，按带方向的水相对运动平移；转弯使用前后姿态的中间朝向投影，避免旋转反复拉伸纹理。保留船侧浪面和尾迹共用端点的连续衔接。
- 完成修改文件语法与本地 1.21.1 API 核对，离线检查覆盖 29,700 组角度插值、7,200 组喷雾方向/速度、9,000 组旋转纹理采样；既有交互和补给回归检查通过。记录见 `docs/wake-motion-0.10.16.json`。未编译、未启动 Minecraft，慢放画面和实际帧率仍需实机验证；现有地图无需重新生成。

## 0.10.15：船体点击与船内换座修复

- **恢复换座**：取消对当前乘坐船只的交互跳过。不蹲下，对准附近空座位所在的船舱右键即可换座；直接更新座位，不下船再上船，保留驾驶员身份及乘员顺序。船上对准补给箱或工作台时，仍优先使用对应方块。
- **左键船体选取**：使用完整船体的空间索引和实际船壳选取，修正长船两端被原版实体查询遗漏、倾斜/翻转后可见船体超出服务端交互范围的问题。选取跟随客户端显示姿态，排除旋转包围盒空白角落；攻击继续使用原版伤害流程和已有改装船掉落逻辑。
- **上船判定**：传递实际点击的船体局部位置，由服务端校验距离及遮挡，避免移动船只在客户端和服务端两次射线检测间错位而拒绝上船。变形、满员、没顶或刚下船的原版冷却导致无法上船时，提供简短提示。
- 版本升级为 `0.10.15`，多人游玩请同步更新客户端与服务端。现有地图直接可用，无需重新生成或导入。
- 完成 Java 语法、本地 1.21.1 API 核对及离线几何回归检查，覆盖 1,152 组姿态和 3,456 条船壳射线；补给交互的既有离线检查通过。结果见 `docs/boat-interactions-0.10.15.json` 与 `docs/refinement-check-0.10.15.json`。未编译、未启动 Minecraft，实际破坏掉落、移动上船及多人换座需实机确认。

## 0.10.14：增强船浪与向外传播

- 提高普通航行、转弯侧浪与拍水浪的基础强度；船侧浪面更宽、浪峰更明显，继续沿实际船体和航迹平滑衔接。
- 船侧水面扰动范围由船舷外 3.8 格增至 5.6 格，船尾压力尾迹长度约增至原来的 1.5 倍；拍水扰动半径约增加 40%。降低水波阻尼，提高向外传播速度，波纹扩散得更远后逐渐复原。
- 脱离船尾的浪迹最长消散时间由 3.8 秒延长至 4.6 秒，拍水涟漪由 2.3 秒延长至 3 秒，并增加横向扩散。沿用原版水色和半透明浪面，不额外加密网格或提高粒子、世界采样预算。
- 本次仅调整客户端水浪效果，现有地图直接可用。完成修改文件语法和离线波动方程稳定性检查；未编译、未启动 Minecraft，最终浪高、覆盖范围与帧率仍需游戏内验收。

## 0.10.13 / 地图 V11：一站一物资与船内补给交互

- **补给规则纠正**：每处补给点只有一种资源。四站依次提供木铲 6 把、木船 2 艘、河豚 4 桶、河豚 4 桶；仍按每位玩家、每局独立领取。绿港是木铲、蓝港是船、黄港是河豚。
- **在船内领取与合成**：箱子移到水边第一排，木船港的工作台与船箱直接相邻。乘船靠泊后，空手对准箱子或工作台右键即可。修正自定义船体选取抢占方块右键的问题，使用原版方块交互距离；真实方块墙壁和其他船只、实体仍会遮挡，自己的船与同船乘员不再截获这次方块点击。自定义右键也遵循原版 4 tick 的使用间隔。
- **复查修正**：压缩和形变过程中的河豚网格不再错误合并前后舱边界；安装射线按实际交互手判断工具，避免副手河豚桶改变主手木铲选取。补给容量计入可以合并的副手物品，空副手不计为自动收纳空位。新存档补齐 `DragonFight` 默认数据，避免加载时报告缺少字段。
- **独立存档 `Puffer_Rally_Mountain_V11`**：保留约 4.66 公里路线、地表细节及雕塑；新地图单独导入 `run/saves`，旧存档和游玩进度保留。
- 验证范围为 Java 语法与本地 1.21.1 API 核对、接缝与容量离线计算、实际 Anvil 方块检查及补给靠泊预览；未编译、未启动 Minecraft，船内领取和工作台界面仍需实机确认。结果见 `docs/refinement-check-0.10.13.json` 和 `maps/Puffer_Rally_Mountain_V11_static_check.json`。

## 0.10.12 / 地图 V10：接缝安装、精细浪迹与补给水港

- **河豚安装**：桶的射线选取改用敞口船壳，船舷上沿不会被不可见的船顶平面挡住；补齐长船接缝、宽船列边界的网格，两侧接缝归到同一个安装位，保留原有河豚坐标和存档。底部仍需要从船底瞄准，预览与服务器共用网格计算。
- **手持木铲快捷安装**：蹲下持普通木铲右键一侧船舷，安装当前交互手里的整叠，只扣实际装上的数量。容量不足时余量留在手上，不读取背包或另一只手的木铲。巨大桨仍逐个按预设位置安装，创造模式不扣物品。
- **水花与尾迹**：侧浪改为不对称的缓坡浪面和薄落水唇，增加连续细褶和更细的飞滴；新贴图分别表现薄水丝、浪尖泡壁与尾迹碎沫。尾迹沿实际航迹扩散，多组次级波纹缓慢回落，泡沫宽度错开变化，衰减时间延长到约 3.8 秒。沿用共享端点避免长船转弯接缝，仍使用原版水色、场景光照及透明渲染。
- **性能排查**：移除水面网格跨 16 格边界时同步扫描整片水域的流程，缓存重叠区域，每刻最多扫描 128 个水格，未知区域继续显示原版水。移位复用数组，避免整组高度场临时克隆；波面共享节点的曲线在帧内缓存。数据包不再每刻检查所有补给箱 NBT，清场检查与计时显示降至每 5 tick。日志没有持续卡服警告，但未实测 FPS，首补给点的实际改善仍需复测。
- **独立地图 `Puffer_Rally_Mountain_V10`**：四处可直接驶入的水港，入口灯箭头、黄白遮棚及高灯标，岸边三种分箱补给：河豚 4 桶、木铲 6 把、木船 2 艘。每人每局每箱独立领取，按真实背包堆叠容量检查；装不下不扣资格。靠近箱子空手右键，无需下船。补给附近检查门延伸到外岸，水港内经过检查门也会计入进度。
- **地表细节**：连片苔地、粗泥、碎石滩与分簇花草，增加 282 组苔石、倒木、桦木、花灌丛及岸边小灯，避开航道、近道和水港。保留 V9 的七组精雕巨构、村居、火山湖与约 4.66 公里总赛程。新存档单独导入 `run/saves`，旧地图和玩家进度保留。
- 完成 Java 语法/API、网格接缝、数组移位、物品容量及存档离线校验；未编译、未启动 Minecraft。实际浪花画面、帧率和多人领取需实机验收。代码检查结果见 `docs/refinement-check-0.10.12.json`，地图校验及预览位于 `maps/Puffer_Rally_Mountain_V10_*`。

## 0.10.11：翘头推进与喷气升力修正

- 普通划桨始终沿水平方向推进，越过浪头、冲出斜坡或弹跳离水后，按住前进不再沿上翘的船头持续增加升力。
- 保留各面河豚喷气原有的水平加速强度；垂直分量单独按河豚数量及船体、乘员的总质量计算，侧喷的高速加成不再直接转成向上推力。少量河豚不能持续克服重力，数量足够且姿态合适时仍能喷气升空。
- 翻滚力矩使用实际施加的推力，避免平移已降低升力而转动仍按旧推力计算。弹簧跳跃能量与锁钩移动规则保留，地图无需重新导入。
- 完成 Java 语法、1.21.1 API 签名与离线推力计算检查：39,000 组姿态、数量和质量组合的水平喷气分量保持一致。未编译或启动 Minecraft，实机请重点比较翘头按 W、少量侧喷离水、大量侧喷起飞及重载船的表现。离线结果见 `docs/propulsion-balance-0.10.11.json`。

## 0.10.10 / 地图 V9：精雕巨构与 16 件配件堆叠

- 七组空中巨构重新制作。双龙重建顺着颈部朝向的头骨、长吻、眉骨眼窝、分叉角、鬃须和鱼鳞腹甲；凤凰重建弯颈钩喙、三层独立收尖飞羽、折叠爪和七根长尾羽。
- 星海鲸加入宽鳍、尾叶、喉褶及侧身星图；水母使用薄伞膜、细触须和宽褶带；云帆舟加入空心船舷、铜肋、三桅鼓帆、索具、船舱及导航轮；河豚改为圆角体形、短棘、鳍纹及气泡；星轮巨桨补充双轨刻度、镶金桨叶和宝石节点。
- 新地图为 `Puffer_Rally_Mountain_V9`，单独导入开发环境 `run/saves`。保留旧版存档，主线仍为 4.66 公里，保留海岸巨型珊瑚、村居与死火山口湖。雕塑为静态方块，未增加持续运行的展示实体。
- 河豚桶和木铲均最多堆叠 16 件。只有组件相同的物品能合并，名称、耐久、附魔或巨桨尺寸不同的物品保持分开。河豚装卸仍每次一桶。
- 保留木铲耐久。挖掘、削平道路或攻击造成损耗时，仅手中的一把消耗耐久，其余放回背包，满包则掉到身边；同时适配已有损耗的木铲合并，避免原版单件工具入包逻辑覆盖物品。通过 `getMaxCount()` 提供工具堆叠容量，保留 1.21.1 存档校验所需的原版耐久组件组合。
- 完成 Java 语法、1.21.1 API 签名和地图几何／净空／照明离线检查；未运行 Gradle 编译或启动 Minecraft，实机编译和游玩验证仍由玩家完成。

## 地图 V8：双龙戏珠、空中巨兽与死火山湖

- 独立存档 `Puffer_Rally_Mountain_V8`，路线仍为 4.66 公里。替换重复的小型空中装饰，沿途分别设置双龙戏珠、星海鲸、金凰、水母、云帆巨舟、河豚月球和星轮巨桨七组大型景观。
- 双龙采用青玉与冰蓝两套配色，一条昂首上升、一条盘旋俯冲，共逐发光龙珠；龙躯直径增至约 19 格。金凰翼展约 160 格、水母伞盖直径约 76 格、巨桨星轮直径约 114 格。姿态差异由静态方块雕塑呈现。
- 海岸新增 14 座巨型珊瑚，包含不同高度、配色及分枝的扇形、鹿角形、层台形造型。与主线、近道和盘山建筑保持间距。
- 雪山顶改为死火山口湖：湖口由约 86×68 格收紧至约 54×44 格，增加宽厚雪脊和玄武岩／凝灰岩分层内壁，湖心深约 13 格，无熔岩。保留山间村居与植被。
- 存档方块、赛道净空、连续桥墩、环形湖壁和照明完成离线校验；预览由实际存档方块绘制。新地图单独导入开发环境 `run/saves`，保留旧版存档。未编译或启动 Minecraft。

## 0.10.9 / 地图 V7：河豚整桶装卸、简明计时与云龙山居

- 替换所有旧的 `WorldView.isChunkLoaded(BlockPos)` 调用，改为查询区块管理器已可用的区块，保留缺失区块时停止计算的保护。编译配置启用具体的弃用位置提示，便于后续依赖更新定位，不隐藏警告。
- 非创造模式安装直接消耗河豚桶，不返还水桶；重锤拆卸直接返还河豚桶，不再查找水桶或生成活河豚。背包满时掉到身边。创造模式安装不消耗、拆卸不返还。
- 赛道侧栏直接显示“用时 · 秒”，内部仍保留 tick 精度；精简开局、检查点、补给、偏航与救援提示，详细操作保留在指南里。
- 新地图 `Puffer_Rally_Mountain_V7` 保留 4.66 公里路线、全部关卡和照明。上空加入三条 440/500/540 格长的玉色巨龙及五组悬浮紫晶桨环；龙珠、腹鳞与星环带发光材料，附近可见少量魔法光点。
- 盘山周边新增 11 栋山间屋舍、105 株针叶树和 183 组灌木，补充暖窗、烟囱烟气、摊篷、菜圃、草垛与晾衣架。建筑基础落在山体上，避开水道、桥墩、隧道和导航箭头；主体为静态方块，不使用大量持续运行的展示实体。
- 旧存档只迁移提示与计时显示函数，并备份原文件；新增地形造景在独立 V7 存档内。未编译或启动游戏，Java 语法、API 元数据及地图 NBT/净空/照明做离线检查。

## 0.10.8：完全入水后恢复浮力

- 静水和流水中的完全没顶状态改用船体浮力，避免回退到原版沉船分支。无论是否倾斜、是否刚结束弹跳，深水中的船都会受到向上的净浮力并逐渐上浮。
- 浮力、回正力矩及弹跳接水检测共用连续水层采样，不再把相邻水方块之间的边界当成水面；每个采样点只额外检查上方一格，不扫描整列深水。
- 浮力大小随浸水程度封顶，避免深度越大反弹越高。保留水面吃水线、弹跳能量、划桨和河豚喷气参数；钩爪锁定仍由绳长约束控制。
- 未编译或启动 Minecraft。实机请检查压缩/展开船、水平/倾斜入水、静水/流水，以及弹跳落水后的上浮与再次起跳。

## 0.10.7 / 地图 V6：检查点救援、弹跳修复与精简赛程

- 主线总长约 4.66 公里，包含约 3.63 公里平地和约 1.02 公里盘山水路。盘山缩为 1.1 圈、半径约 150 格；保留水闸、两段冰面陆路、钩爪峡谷、四个个人补给站、两条近道、两座隧道、两段涵洞、终点港池与雪山湖。
- 救援先用临时区块票据等待目标区域可运行，再一起移动船和乘客，保留改装与座位；清除旧钩爪、弹簧推力及运动状态。等待期间不会重复请求或重复罚时，超时取消且不扣罚时，退出/断线清理等待状态。
- 弹跳期间的划桨推力保持水平，避免翘头时按住前进不断获得升力；按实际船底接水与稳定落地判定再次起跳，保留空中展开影响跳高的操作。河豚推力和普通划船力度保持现有数值。
- 盘山增加朝向水道的内壁灯、隧道顶灯，并用无碰撞的原版光源方块补足水道中央及港池照明；不降低水深，也不阻挡船只通过。
- 新存档为 `maps/Puffer_Rally_Mountain_V6`。旧存档仅升级救援数据包函数并备份原文件，地形及玩家进度保留。需要使用模组 0.10.7；未编译或启动游戏，实机验证仍由玩家完成。

## 地图 V5：终点港池、雪山湖泊与护栏照明

- 比赛准备倒计时改为 10 秒（200 tick），显示 10 至 1，结束后在岸上起跑。
- 独立存档 `maps/Puffer_Rally_Mountain_V5`，主线约 9.15 公里。末段水面转为平缓平台，终点线后增设约 80 格缓冲港池、黑白终点标线、环绕步道及封闭尽端，新增落地支撑，消除水道直接断开的结尾。
- 雪山顶增加 Y=179 湖泊，采用不规则岸线、深浅变化的湖底和积雪湖岸。
- 盘山护栏增设石质灯柱与灯笼，水底补充导向照明；终点步道也布置灯具。保留原有隧道、涵洞照明和发光箭头。
- 离线校验覆盖倒计时函数、港池水面与尽端、湖泊及岸线、灯具实际方块、旧有桥墩和箭头；未编译或启动 Minecraft。旧 V4 存档保留。

## 地图 V4：桥墩落地、完整箭头与缩短赛程

- 独立存档 `maps/Puffer_Rally_Mountain_V4`，约 9.15 公里，比 V3 缩短约 18%。删去重复折返和部分长直线，盘山段由 2.2 圈缩到 1.65 圈，保留两条近道、两座隧道、两段涵洞与全部个人补给站。
- 盘山桥梁改用错位门式桥墩：选址避开下方航道及标识，以横梁连接上层桥面，完整墩柱落到地下地基。桥墩在通道挖空完成后生成，不再被下层水道截断。
- 箭头使用逆向方块栅格化，箭身、箭头和底板完整铺设；涵洞和隧道采用嵌入水底的发光实心箭头，放在同一水位平台中部，避免台阶切断图案。
- 清除四个补给码头入口处的沙土隔条，并检查水面至码头的通行空间。岸上起跑、个人补给与偏航提醒保留。
- 离线校验新增每根桥墩从地基至横梁的实体连续性、每个箭头的四邻接连通及实际方块完整性、各补给入口的净空检查；未启动 Minecraft。旧 V3 已游玩存档保留。

## 地图 V3：曲水近道与螺旋盘山终关

- 独立存档 `maps/Puffer_Rally_Mountain_V3`，主线约 11.1 公里，沿用模组 0.10.6，不修改船体物理。保留 V2 已游玩地图；生成器复用既有双人比赛、个人补给、背包与清场规则。
- 后段新增 12 个紧凑转弯，水道收放到约 27~41 格宽，提供两条宽 11 格的绿色近道，分别节省约 237 / 281 格，途中需越过一格跳栏。检查门避开近道跳过的 U 弯，两种走法均能完赛。
- 主线两岸密集设置黄色箭头，近道用绿色标识。每半秒按当前检查点附近的实际通道检测一次；持续偏离约 1.5 秒后提醒，声音与文字最多每 3 秒一次。合法近道和补给码头均纳入走廊，水平环视不会触发偏航判断。
- 最终水路约 2.9 公里、螺旋环山 2.2 圈，水面 Y=63 至 Y=148，约每 34 格上升一格，采用四格深水槽。上下层按高度判定检查点，桥墩避开行驶空间；两个岩壁隧道净空 8 格，两个涵洞净空 5 格。新增岩山、雪顶松树、瀑布、桥墩、护岸、灯带和山巅观景台。
- 两艘基础船改为在港口岸上并排发船，倒计时结束后选手自行下水；救援仍返回水上检查点。初始重锤、空首格、2 个河豚桶及 4 把木铲保留。
- 新生成器 `tools/make_mountain_race_save.py`，离线校验 `tools/check_mountain_race_save.py`；输出存档、ZIP、路线图、盘山方块俯视图和游玩指南。未启动游戏；逐级水位的实际浮力、窄道驾驶与多人比赛仍待实机验收。

## 0.10.6：遮水回退、精简仪表与个人补给

- 撤销后置水面绘制中新增的船舱深度遮罩，排除重复相机变换造成随视角移动的透明水面缺口；保留乘船 360 度环视及原有实体遮水。该回退可能使自定义水波再次进入船舱，透明缺口与船舱表现待实机确认。
- 按要求回退 0.10.4 的喷气推力、空中划桨、起跳支撑及浮力采样改动，恢复此前驱动力；保留 0.10.5 弹跳中展开取消剩余助推和空中发钩功能。
- HUD 精简为形态尺寸、配件数量、钩爪与弹跳、五向气量，去掉重复气量及倒计时，边框随内容高度变化。H 开关，新增 J 循环切换 75%、100%、125%、150% 大小，可在按键设置修改；开关与缩放保存在 `config/longboatlab-hud.properties`。
- 补给站空手右键领取：每人每站每局独立获得木船、木铲、河豚桶各 2 件；预留 6 个空位，不足时不扣领取资格。重锤改为开局自带，快捷栏第一格留空并选中，第二格固定放重锤。
- V2 数据包支持已游玩存档内的补给箱与告示牌按区块加载迁移，不重建地形或覆盖玩家进度。新版个人补给及自动选中首格需要模组与数据包同时更新至 0.10.6。
- 仅做离线存档、语法与接口检查；未编译或启动 Minecraft。

## 0.10.5：弹跳中变形与随时发钩

- 弹跳过程中可按 R 立即展开成长船。弹簧在前 5 tick（约 0.25 秒）随竖向展开逐步释放固定能量：提前展开会取消剩余能量，已经获得的上升速度保留；完整释放后再展开可获得完整弹跳高度。展开位置仍需通过空间检查，检查失败不会扣除剩余弹跳能量。
- 切回压缩形态不会恢复已取消的弹跳能量；撞顶、上升受阻、装上巨大桨或钩爪锁定后停止剩余助推，避免重复触发额外弹跳。
- 钩爪在压缩形态且完全收回时可发射，包括起跳、竖向展开、回缩、下落和切回压缩的短暂形变期间；同一 tick 的空格与 G 可同时起跳发钩。G 收钩、空格解锁和船桨决定射程的规则保持。
- 发射起点跟随当前实际船头；锁定后补偿弹簧和形变造成的安装点位移，保持实际索长与球面半径，不将形变误判为传送。补偿运动被方块挡住时松钩，避免拉长刚性索或穿墙。
- 版本只改模组逻辑，地图不需重新导入。未编译或启动 Minecraft，实际跳高及多人钩爪表现待实机验收。

## 0.10.4：喷气升力与弹跳失控修正

- 底部、前后、左右河豚统一推力公式：每面推力为 `0.018 × sqrt(数量)`，除以包含船长、船宽和乘员的质量，再按船体姿态转换到世界坐标；每面加速度上限统一为 `0.075`。移除侧面额外的高基础推力，侧面翘起时不再获得比底部更强的升力，同一推力也用于翻滚力矩。
- 保留大量河豚使船腾空的机制：世界向上推力必须克服重力 `0.04`，船越重、喷射方向越接近水平，需要的河豚越多。数量少时仍可延缓下落，但不会凭侧面安装绕过重量。
- 普通船桨只在船体接触水面或地面时提供水平推进，离水后不再沿翘起的船头持续施加升力。巨大桨已有的实际触地驱动保持原样。
- 弹跳改用实际船体浸水采样检查起跳支撑，拒绝上浮反弹时利用残留水接触再次起跳；长度决定基础跳高的规则与单次垂直起跳保留。
- 浮力采样覆盖吃水带下缘，修正采样中点刚越出水面时直接丢失浮力的跳变，并让浅吃水状态仍能正常起跳。
- 按要求未编译、未启动游戏或运行驾驶测试；侧喷升空门槛、翘头入水后按 W/空格的表现仍需实机确认。地图无需重新导入。

## 0.10.3：拆鱼回收、自由环视与渐进赛道

- 生存模式用重锤拆河豚时查找背包和副手里的水桶：有水桶则消耗一桶并原位换成河豚桶；没有水桶则在安装位置释放活河豚，可重新捕捉。创造模式沿用不返还物品、不生成生物的拆除方式。
- 玩家乘船取消原版左右视角限制，可水平 360 度环视；保留船体转向及翻滚时的乘员坐标变换。
- 修正船体轻微倾斜就关闭挡水遮罩的问题，按船舷是否没水判断，并在后置自定义水面绘制前补画船舱深度遮罩。倒扣和真正没水的船仍可进水。
- 新地图 `maps/Puffer_Rally_Shared_V2`：约 5.39 公里，前段 25 格宽，中段 41 格，后段平滑拓宽到 57 格；起步补给提前到 140 米，早期跳栏集中在前 255 米，后段取消强制跳栏并延长 1000 格。
- 每人开局仍是一格船，另带 2 个河豚桶、4 把木铲。开局/重置清空参赛者背包（含装备、副手），重发配件及右键工具；重赛清船、散落物与活河豚并恢复补给。未加载的旧实体按轮次延迟清除，无需强载全图。
- 原来已游玩的地图保留。离线存档检查、Java 语法检查不代替实机验收；没有编译或启动 Minecraft。

## 0.10.2：共用大回环赛道与右键救援道具

- 新版存档 `maps/Puffer_Rally_Shared`：两人共用一条约 4.39 公里、41 格宽水道，7 个半径约 90 格的真实转弯，包含回头弯、冰面陆路、弹跳水闸与钩爪峡谷；两岸增加林地、岩石、雪林、海岸灯塔和维修码头。
- 双方从普通单格橡木船起步，左右各一桨、没有河豚。全线仅 4 个共享补给箱，每箱木船、木铲、河豚桶、重锤各 2 件，每局接近时只补货一次；码头设工作台逐步改装。
- 钩爪峡谷移除底部直行道路，加入间距 7 格的金色锚梁、三个高位落脚台与高处必经检查门；坠落自动救援。
- 新增地图专用右键道具：指南针保留改装救援（+10 秒）、追溯指针换基础船（+20 秒）、绿宝石单练、羽毛退出、时钟赛后重开、书查看帮助。通过 Fabric 交互回调向数据包提交固定动作，无需访客管理员权限。
- 存档、路线俯视图、游玩指南与 ZIP 均在 `maps`；生成脚本 `tools/make_shared_race_save.py`，静态检查 `tools/check_shared_race_save.py`。旧版地图及已游玩存档保留。新道具需要重新编译 0.10.2；没有编译或启动游戏。

## 0.10.1：拍水音效与双人体验地图

- 新增船体撞水专用音效：低沉拍击、碎水、回落水流叠加原版高速落水声。播放位置跟随实际撞击点，按入水速度与接触面积调节音量和音高。
- 新增更明显的连续船舷破浪声，按航速增强，停车渐弱，音源放在听者附近的船舷水线。使用“方块”音量分类；水声不提高河豚喷气的高频或音量。
- 同一艘长船的多处同时拍水有 8 tick 合并间隔，全局每 tick 最多 4 次撞击声音，避免接触点重复叠加。音效只由本地视觉事件播放，不额外广播导致多人重复收听。
- 原创音频资源生成脚本：`tools/make_water_sounds.py`。持续破浪是 6 秒循环，拍水是 2.4 秒衰减声音。
- 新增完整地图存档 `maps/Puffer_Rally_2100` 及同名 ZIP：约 2.1 公里、两条对称赛道、11 赛段、16 个比赛补给箱，包含弹跳水闸、冰面陆路、回旋障碍、钩爪峡谷、跃浪台阶和尾喷冲刺。
- 地图自带数据包：双人准备区、同时倒计时、检查点、个人计时/名次、单人练习、保留改装救援、换艇、重赛；起点另设巨大桨和加宽合成体验港。详情见 `maps/Puffer_Rally_2100_游玩指南.md`，路线图见 `maps/Puffer_Rally_2100_overview.png`。
- 生成脚本 `tools/make_race_save.py`，离线存档检查 `tools/check_race_save.py`。未使用或修改已有玩家世界；未启动 Minecraft、未编译模组，实际画面、音量和赛道驾驶仍需实机验收。

## 0.10.0：加宽船、河豚向外堆叠与细化喷溅

- **合成加宽船**：工作台内放入至少两艘同材质、同长度（长度至少为 2）的船，结果保持长度、相加宽度。例如两艘 4×1 合成 4×2，三艘合成 4×3。船体是一体连续的横向扩展外壳，内部横梁仍按长度生成，不增加并排小船的内侧墙。物品与手持渲染跟随实际宽度；第一人称握持向外偏移，避免宽船遮挡中央视野。
- **加长规则**：不满足上述加宽条件时，要求所有船宽度相同，长度相加。单格船仍可合成长船；例如 2×2 与 3×2 合成 5×2。生存合成的长、宽分别最多 16；指令宽度不受生存上限限制。
- **改装保留**：合成保存两侧桨与河豚总数，将底部/前后网格搬到对应横向船位，两侧河豚归到新船外侧。普通桨生存容量按长×宽计算。仍禁止普通桨与巨大桨混装；合成后巨大桨安装点冲突时搬到同侧最近空位，只有该侧已无空位时才拒绝合成，不会吞掉任何一把。
- **并排座位**：普通船展开后每个长×宽船位有前后两个座位，按点击位置就近选择空位；座位、乘客翻滚姿态、碰撞与下船位置均使用宽船坐标。压缩只收拢长度，保留宽度和并排座位；压缩前重新分配空位，乘客超过 2×宽度时拒绝压缩。
- **尺寸适配**：宽度接入客户端同步、存档、放置/掉落/中键拾取、碰撞分块与转弯扫掠、质量和翻滚惯性、浮力取样、普通/巨大桨支点、河豚五面预览/推力/喷气/音源、水面遮罩、凹陷压力场与两舷连续浪迹。钩爪仍位于船头中心，弹跳仍按纵向叠层数计算。HUD 与物品提示显示长×宽；旧存档未写宽度时按 1 读取。
- **河豚向外堆叠**：蹲下拿桶点船面安装第一只；再点已有河豚的侧面可向外继续接，点上表面可增加上方的一排。前、后、左、右均适用，底部继续向下叠放；预览与服务器共用放置计算，重锤可拆卸新分支。旧河豚列保持旧布局，新放置列使用向外堆叠。
- **细飞滴**：两舷浪尖剥离更密的微小飞滴和薄雾，缩小水珠与雾片，减小雾片膨胀幅度。总池上限为 384 飞滴/192 薄雾，随粒子设置降低；连续浪面优先使用射线与水面查询预算，慢速微粒改用当前位置的方块形状检查。
- **喷气音色**：循环资源改成低频为主的柔和气流声，降低高频嘶鸣和音量，去掉单频哨声；按住逐渐起音、松开渐弱，淡出期间音源继续跟随河豚嘴部。

调试示例（权限等级 2）：

```mcfunction
/longboatlab rig give {Length:4,Width:3,NormalOars:1b,LeftCount:24,RightCount:24,Grid:[{Face:"stern",Lane:2,Count:6,Outward:1b},{Face:"bottom",Slot:1,Lane:3,Count:3}]}
/longboatlab rig spawn {Length:4,Width:2,LeftCount:4,RightCount:4,LeftSize:3.0d,RightSize:3.0d,Distance:10.0d,Height:4.0d}
```

新增参数：`Width` 默认 1；`NormalOars:1b` 使用普通桨（不与明确巨大桨列表或 Size 混用），此时 LeftCount/RightCount 可超过生存上限；`Compressed:1b` 用于生成压缩形态（巨大桨仍强制展开）。Grid 的 `Lane` 从右向左从 1 开始，底部和前后面可选横向船位；左右面固定 Lane:1。`Depth` 为向外起始层，默认 0；`Outward` 默认 1，0 读取旧式列步进。`Slot` 仍从船尾向船头从 1 开始。物品放下时继续默认纵向压缩。

仅进行源码/资源静态核对，未编译、未运行 Minecraft 或模拟测试。实机重点观察加宽合成后部件/存档、多名玩家并排乘坐及压缩、宽船岸边转弯和水花、河豚新分支预览与音量过渡。


## 0.9.4：船头柔和起浪、碎沫浪尖与撞水水冠

- **前端过渡**：船头处浪宽、浪高和透明度从零开始，沿船身按平滑曲线抬起；前段增加固定船内坐标取样，单格船也能形成过渡。保留 0.9.3 的共享节点与船尾连接，不重置浪的年龄。
- **减轻塑料片外观**：降低连续水体的不透明度和整体浪高；新增有水丝层次的半透明水体贴图、断续泡沫和小气泡贴图。纹理相位跟随持久节点，浪尖有轻微连续起伏，较强浪尖少量剥离飞滴；新增内容不会让整个水面换成蓝色光影材质。
- **拍水表现重做**：从与接触面积相关的边缘抛出水冠，垂直拍水偏径向扩散，斜向高速拍水加强迎水方向喷射。水冠先扩张上升，再分成水舌与细丝；接近抛物线顶点时错开发射粗水滴、细飞滴和少量细雾。取消撞击中心立即喷出大团雾的做法，水滴落回水面仍可产生小涟漪。
- **尾波形态**：自由尾迹横向传播速度随航速增加并随年龄减小，参考深水重力波发散规律，速度有界；主浪回落后转为较弱、较宽的次级波纹和分离泡沫条纹，逐渐耗散。强尾浪短时间内有少量低矮半透明水雾，不持续覆盖为雾墙。这是视觉近似，不宣称完整求解 Kelvin 船波或浅水流体。
- 新增浪尖碎滴/细雾共用每刻 6 个发射预算，仍受总粒子数量与查询/碰撞预算限制；前端细分使船侧活跃浪段上限由 64 调整为 80。资源生成脚本为 `tools/make_wake_ribbon_texture.py`。

建议验收：单格与长船前端是否平滑起浪、直行/转弯时船侧—尾迹接点、低速与高速尾浪层次、垂直/斜向拍水后的水冠破碎与飞滴回落。**仅完成源码/资源静态检查，未编译或运行 Minecraft；实际画面和性能仍需验收。**

## 0.9.3：重做船侧与船尾浪带的连续连接

- 修复 0.9.2 每刻重建船侧浪并重置 `clock - 4` 出生时间造成的周期性浪高/宽度变化。改为持久节点保存上一刻与当前的位置、浪高、透明度；水面高度和浪强平滑更新，渲染帧只插值。
- 船侧与自由尾迹共用同一节点及横截面绘制方式。船尾旧节点脱离船体留在世界中，新船尾节点同时作为船侧段和连接尾迹段的端点，连同水色、光照和透明度一起共享；停用重复的每两刻独立船尾浪带发射器，保留其水面扰动和少量飞滴。
- 相邻浪段共享取水高度缓存，每个节点每刻只更新一次，取消船侧每刻更新、尾迹每三刻独立插值造成的接缝。尾迹脱离时从实际船尾形状开始衰减，不再从零浪高重新长出。
- 船身取样以稳定的船内坐标为键；普通长船不因相机移动而重编号。转弯时低速的中部节点保持连续，只平滑降低浪强；前后方向有低速滞回，避免近乎原地转弯时尾迹在两端跳跃。
- 当前船侧取样预留预算，不足时保留完整旧浪带，不把“未查询”当成“干地”删掉片段。岸边仍检查端点/中点与碰撞。移除上一版按局部重叠直接整段删除历史浪的做法，采用正常船体深度遮挡；极端急转的尾迹交叠仍需游戏验收。
- 上一版第一人称握持调整保留。停止更新的船侧浪带会脱离并淡出；传送、变形断开连接，避免跨越跳变位置拉出长条。

建议验收：一格船和 16 格长船匀速直行、连续左右转弯、近乎原地旋转、停车与倒车；重点观察船侧—船尾接点在同一帧是否连续、有无每刻闪动，以及岸边和较低帧率。**仅完成静态源码和资源检查，未编译、启动 Minecraft 或运行模拟测试，视觉效果仍需实机确认。**

## 0.9.2：第一人称握持与长船转弯破浪

- 第一人称持船独立设置为 0.32 倍显示比例，向持握侧、下方和前方偏移，长轴向远离眼睛的方向延伸。左右手镜像处理，一格船不再以世界尺寸横在眼前；保留真实长短比例、改装外观和物品栏现有展示比例。
- 两舷新增沿实际船长的连续水线取样，使用当前/上一刻各船舷点的位移，包含转弯引起的局部速度。外侧迎水浪更强，内侧退让浪更弱，近乎原地旋转时也可触发；不再只按船体中心的平移速度生成船头尾迹。
- 船身旁的浪带随实际水线更新并逐帧插值，自由尾迹改从当前行驶方向的船尾释放。船体重新扫过旧浪带时裁除被覆盖段，避免浪带穿过转弯中的长船。传送、变形会重置浪带连接。
- 只处理附近最多 8 艘船，每侧最多 17 个取样点，实际新绘制船身浪带每刻最多 64 段；保留水面查询和碰撞预算，并检查外缘与岸边，避免随指令船长无限增长。超长船仅绘制相机附近船身。

建议验收：左右手单格/长船的第一人称静止、行走和挥手；16 格长船直行、急转、原地转向、倒车、岸边和展开/压缩。**仅做静态源码核对，未编译或运行游戏。**

## 0.9.1：创造拆卸、柔化水幕、持续航迹凹陷与普通划行破浪

- **航行浪花重新塑形**：停用重复叠加的扇形航行水片，利用两舷连续航迹绘制六段弯曲横截面的立体浪带。前段逐渐抬起，外缘形成窄泡沫浪尖，随后回落并过渡到水面余泡；新增无沿程透明接缝的水体纹理。航行不再生成水雾，只有较强破浪保留少量飞滴；撞水仍保留曲面掀浪，强撞击才有少量水雾。
- 创造模式使用重锤拆卸河豚或普通／巨大桨时只移除部件，不向背包返还物品，也不因背包满而掉落。生存模式仍返还对应物品。
- 水幕增加曲面细分，以相关波形取代相邻节点独立随机高度，加入连续鼓起／卷边、弹道帧插值；水幕基部每刻取样并插值，撕裂与落水透明度平滑渐变，减轻硬切缺口和直板外观。降低水体不透明度，缩窄泡沫冠，使其贴合弯曲水幕。已有特效总量、碰撞和查询预算保持有界。
- 撞水检测对内置水面模型使用原版水位和向上法线，避免动态斜坡把水平前进反复判为新撞水。水滴／泡沫仍跟随视觉波面。撞水改为衰减的下压目标，不再直接跳改高度和速度；作用范围覆盖撞击前一刻的移动方向。
- 持续船体作用沿前一刻位移扫掠，压力保留在经过的世界网格，转弯后旧航迹不会跟着船转向。压力采用平滑加载、短暂停留和慢释放；波传播速度降低、阻尼提高，目标是清晰的压下—低谷—回填—余波过程。高速凹陷目标增强，实际深度受水深与 1.6 格下凹上限约束；不是每次撞击都会达到上限。
- 普通划行也产生轻微双舷破浪：0.12 格/刻开始，低于 0.09 停止；水幕高度、水滴喷速、泡沫透明度随速度渐强。视觉水面下凹不会反复切断破浪发射，泡沫允许跟随更深的波谷。

建议验收：创造模式空／满背包拆卸各部件、生存返还；普通船直行和停车、高速转弯后的弯曲航迹、连续拍水后慢回填；第三人称水幕曲面和渐隐。**仅修改代码并做静态核对，未编译、启动 Minecraft 或运行模拟／性能测试。**

## 0.9.0：原版水材质、持续船体凹陷与尾部回填

- **还原原版外观**：移除局部水面的天空反射、深蓝吸收、屏幕折射、强高光及不透明合成。使用原版动态水纹、生物群系水色、环境光照贴图、顶面亮度、贴图透明度与雾效；水纹从此前每 4 格重复改为原版每格重复，并匹配原版 UV 内缩及水面微小高度偏移。法线只提供轻微、左右对称的坡面明暗，静止平面不加额外光影材质。
- **实际位移而非贴图假洞**：保留细分网格下沉、隆起及波动传播，侧视视差和深度遮挡来自几何本身。没有使用平面视差贴图冒充凹陷。高度编码扩展到 ±2 格，防止更深的波谷被编码截断。
- **持续排水与尾迹**：新增仅客户端使用的 `WaterSurfaceModel.acceptHull`，根据船底端点和实际航行方向持续形成船身低谷、两侧隆起与尾部扩散低谷；倒车时对应尾迹跟随实际后端。松开推进、船只离开后由波动场回填、回弹和衰减。船只浮力、河豚推力及服务器物理不受修改。
- **深度与过渡**：高速时加强凹陷目标，实际下凹上限由水深限制，最多 1.25 格，避免浅水波谷钻进河床；波峰上限 0.90 格。局部边缘和干湿边缘均采用平滑衰减。慢速可有轻微排水，不改变高速喷溅的 0.48 格/刻触发阈值。
- **有界开销**：每刻最多 8 艘附近船提交持续压力，计算范围固定，不按指令船长增加网格数量；静态网格及高度/水色/光照纹理复用，移除每帧全屏颜色和深度复制。未经实测，不提供帧率提升数值。

本版仍适配原版 Fabric 渲染路径；Sodium/Iris 会回退。局部网格与玻璃等其他透明物体的排序、极佳画质和第三方资源包仍需游戏验收。建议检查静止边缘水色、昼夜/火把光照、第三人称侧视凹陷、加速尾迹、停车回填与浅水。**仅完成代码和静态核对，未编译、未运行游戏或模拟测试。**

## 0.8.2：修复水面挖空与蓝色网格随视角漂移

- **直接绘制漏掉相机旋转**：0.8.1 日志显示 shader 加载与网格提交成功，但直接 VBO 绘制传入了 `context.matrixStack()` 的局部实体矩阵。1.21.1 的相机旋转在世界视图矩阵中，直接 VBO 不会自动继承它，导致原版地形遮罩与替换水面不在同一个坐标系。现在捕获地形的 `positionMatrix()` / `projectionMatrix()`，让水面网格、法线视图变换和折射深度重建使用同帧矩阵。相机位置仍只在 `PatchOrigin` 中扣除一次。
- 折射分辨率参数改为自有 `SceneSize`，避免引擎在绘制时把 `ScreenSize` 覆盖成窗口尺寸，确保屏幕取样对应实际复制的场景附件。
- 本补丁修正渲染坐标与取样参数，不改变撞水、高速航行的触发条件及模拟范围。静态核对了 Fabric 回调、Minecraft VBO/地形绘制调用及 shader 参数，未编译或启动游戏。

建议验收：在开阔水面触发撞水后停船，原地转动视角并切换第三人称俯视/侧视，凹陷应固定在撞击位置传播和消退，替换区域不应留下缺口或随视角滑动的蓝色平面；再检查移动相机及窗口缩放。

## 0.8.1：修复动态水面未启用

- 本地 `run/logs/latest.log` 确认 0.8.0 的 `water_surface.fsh` 使用 GLSL 保留字 `flat` 作为变量名，导致片元着色器编译失败，整套动态水面回退为原版。将该变量改为 `flatWaterColor`，修正此加载阻断。
- shader 加载失败或因 Sodium/Iris／启动参数关闭时，进入世界后给出一次聊天提示；资源重载时重新更新提示状态。日志分别记录 shader 加载完成、地形替换生效且首次提交位移网格，便于区分加载失败与尚未触发水面效果。
- 动态水面仍由附近的撞水或超过 0.48 格/刻的高速航行触发；静止、普通慢速航行不会凭空产生凹陷。原有渲染器兼容范围不变。

本补丁仅检查现有运行日志并修改、静态核对源码和资源，未进行编译或启动游戏；修复后的实际效果由游戏验收确认。

## 0.8.0：真实局部水面凹陷与传播波动（第一轮）

- **实际水面位移**：默认在标准 Fabric 渲染路径启用玩家附近 48×48 格、半格间距的局部高度场（97×97 节点）。撞水压低中心水面、抬起周围水环，再产生回弹、传播、叠加与衰减；高速双舷破浪持续注入较弱扰动。下凹/抬高有 -0.70/+0.90 格保护范围，极端指令船不会无限放大波面。
- **替换原版平水面**：自定义地形 shader 只遮掉局部遮罩内对应水位的水面顶面，再用细分网格绘制真实起伏。不会简单叠一张被原版水面覆盖的凹陷网格，也不需要每刻重建区块。流动水、含水实体方块、岸边不满足平坦条件的水和不同水位暂留原版。
- **水面 shader**：高度纹理驱动顶点位移与波面法线，加入屏幕空间折射、水深吸收、Fresnel 反光、近似天空反射、细波高光和波峰泡沫。水面先写入实际深度，再绘制此前的喷气、水滴和泡沫，保持物体遮挡。场景颜色/深度复制到独立 FBO，避免读写同一纹理。
- **预算控制**：单一有界模拟区域，三个固定子步/刻，静态网格复用，当前/上一刻高度以 16 位精度打包上传，无 GPU 回读；场景缓冲随窗口尺寸复用。区域按 16 格移动并保留重叠部分的世界坐标波形。新增 GPU 场景复制与像素着色仍有成本，未进行实测性能比较。
- **开源参考**：查阅 [Evan Wallace WebGL Water](https://github.com/evanw/webgl-water) 的高度/速度场方案（源码标明 MIT），保留作者归属与许可文本并打包到 JAR；另查阅 [jquery.ripples](https://github.com/sirxemic/jquery.ripples) 的波纹与折射思路，未复制其源码。完整研究依据和实现说明见 [docs/water-surface-design.md](docs/water-surface-design.md)。
- **兼容范围**：第一轮优先原版 Fabric 渲染器。检测到 Sodium/Iris 或 JVM 参数 `-Dlongboatlab.disableWaterSurface=true` 时回退到原版水面和已有浪花，防止未经适配的管线把水面遮没。自定义 shader 加载失败时也保留原版水面。画质、资源包与渲染模组的实际兼容性尚待游戏验收。

这轮实现的是**真实位移网格及局部波动**，尚未让凹陷反过来改变服务器浮力、游泳或水下雾判定；不支持翻卷浪、瀑布或水方块迁移。反射为近似天空，折射只包含当前屏幕中的场景。区域边缘、资源包与切换水位的细节需要后续打磨。

建议从大面积平静湖面测试船底拍水、船头扎水、连续加速破浪、多个撞击叠加，再检查第三人称俯视、岸边与玻璃遮挡、资源重载、窗口缩放和极佳画质。**仅完成代码/资源与 API 静态核对，未编译 Java/GLSL、未运行模拟测试或启动 Minecraft；不宣称已达到实测画质或帧率目标。**

## 0.7.3：翻滚后的推进方向、浪花单侧变暗及开销优化

- **推进方向**：此前接地/水面划行使用 yaw 单独计算方向，船体翻滚后实际船头还受 pitch/roll 影响，两者可能相反。普通桨和巨大桨陆地牵引改用实际船头的水平投影；接近竖直、投影长度过小时沿用最近有效方向并跟随 yaw 变化。接地推进仍没有额外向下/向上的分量，空中划行和河豚喷射继续跟随完整船体姿态。已有反向惯性不会被瞬间清空。
- 整圈翻滚回正后，倾斜判断使用周期角度误差，不再仅因角度累计为 2π、4π 等而持续启用倾斜环境分支。
- **浪花单侧变暗**：左右镜像浪带的三角形绕序会生成反向法线，原实体着色器按法线进行方向光调制，容易把一侧压暗。水幕/泡沫/水滴改用一致的向上散射光照法线，保留环境光贴图和水色，不使用全亮白色。仍保留上一版的末尾透明绘制和正常深度遮挡。
- **水面查询缓存**：原版水面提供器每刻按方块缓存干湿、表面高度、碰撞遮盖和流速，同一区域的浪带/水滴/撞击采样复用结果。下一刻清空以响应水体和方块变化；未来自定义水面提供器不受此缓存影响。
- **稳态航行快速路径**：使用平移与角度变化的保守上界判断是否可能向下拍水，原版平水面上稳定横向航行无需每刻执行 8×7 个拍水探针；持续双舷破浪仍正常工作。快速旋转/下坠及自定义水面模型保留完整扫描。
- **特效开销**：普通破浪满粒子档的扇形节点由 13 降到 7，纵向细分由 3 降到 2；撞水大水幕保留原精度。普通破浪主要使用连续泡沫带和水滴，仅强破浪产生细雾。水幕水体/浪冠共享本帧插值结果；远处失效特效在碰撞查询之前剔除。
- **喷气开销**：附近喷气船只的空间检索移到每刻，喷口列表和数量倍率在同一刻内复用（相机明显移动或船体伸缩时更新），各段粒子使用共享旋转基底，减少逐喷口重复三角函数计算。巨大桨没有输入且无水平运动时跳过无用的接地牵引探针。

建议验收：长船翻滚半圈/整圈后贴地或回水面，W/S 推力是否和船头一致；巨大桨同样场景；双舷尾迹在不同视角和光照下的对称亮度；有/无喷气及强拍水的帧率与 TPS。**仅修改源码并做静态检查，未编译、未启动 Minecraft、未跑性能测试；上述优化未提供实测提升百分比。**

## 0.7.2：修正水面背景透明覆盖，连接破浪尾迹

- **透明层顺序修正**：喷气、水幕、水滴、水雾和泡沫统一移到 Fabric `WorldRenderEvents.LAST`，在原版水面与“极佳”画质的透明合成之后向主帧缓冲绘制。使用独立、可复用的 Immediate 缓冲区并在同一回调中提交完成，不再依赖此阶段不可用的 `context.consumers()`。特效层仍启用半透明混合、双面绘制和 LEQUAL 深度测试，但只写颜色、不写深度，减少透明四边形互相遮住的情况；方块和船体仍参与遮挡。
- 喷气移出船实体绘制回调，改为在附近船只中绘制当前/渐隐喷口；使用同一帧船体姿态、相机相对坐标和原有 64 格粒子距离裁剪。原实体渲染入口不再重复绘制喷气。缓冲区在帧间复用，并在客户端关闭时释放。
- **连续破浪**：每侧航行水花更新间隔从 3 刻降至 2 刻，同时减少单次飞散水滴数量。航行事件新增稳定的 `wakeSide` 标识，按来源船 UUID 和左右侧连接水面泡沫波带；不再给每个航行事件生成一圈孤立的大涟漪。保留原 `WaterImpact` 构造参数的兼容重载，撞水事件默认 `wakeSide=0`。
- **余波渐散**：波带保留在世界坐标中，较老的端点向外扩散得更远，构成船后延伸的 V 形尾迹。宽度随年龄增大，透明度按平滑曲线在约 3 秒内降到零；端点采用独立渐变透明度，相邻段共用相同帧时间计算位置，减少接缝。停止航行后已有尾迹继续淡出。撞击产生的环形余波和水滴落水小涟漪仍保留。
- 传送、突然反向和长时间断流不连接跨越空白区域的波带。波带每 3 刻检查边缘/中点水面，遇到岸边、未知水面或查询预算不足时裁除，不穿过陆地。新增波带上限随粒子设置为 64/128/192 段，沿用现有查询和碰撞总预算；大量同时喷射时仍会降级。
- `WaterSurfaceModel.render` 预留接口仍位于 AFTER_ENTITIES，供未来真实水面网格使用；本次仅将喷雾与泡沫等附属特效移到末尾绘制。该改动不替换第三方光影的水体折射或水下渲染逻辑，其兼容性仍需实际游戏验证。

建议验收：第三人称从船顶俯视水面时的喷气、双舷水幕及泡沫可见度；同一位置从侧面观察；岸边和船体遮挡；快速/精美/极佳三档画质；连续直行、转弯和停止后的尾迹展开与渐散。**仅修改代码并进行静态源码、资源与 1.21.1/Fabric API 核对，未编译、未运行测试或启动 Minecraft。**

## 0.7.1：半透明缓流喷气、双舷破浪与拍水可见度

- **喷气柔化**：移除不透明亮芯叠层和饱和蓝色硬边，改为 32×32 柔和渐变透明贴图，降低整体不透明度。仍按各面河豚数量独立放大长度与宽度。
- **流速与停留**：喷气从喷口到尾端的传播阶段由 4 刻延长为 8 刻，接近尾端时减速，再保留 4 刻（约 0.2 秒）的扩散阶段。尾端粒子轻微散开并淡出，避免挤成不透明圆斑。松开喷气后已有粒子最多继续存活约 0.6 秒，声音和实际推力仍随喷气停止。喷口两段继续使用当前帧位置；沿途粒子依发射时间取历史姿态，不让整个尾迹跟着船的旋转硬转。
- **双舷破浪**：航行水花与撞水取样分开，按当前前进/后退方向选择迎水端，在左右船舷各自与水面的交点生成，禁止将两边合并到船体中心。两片开放扇形水幕向舷外和后方掀起，水滴也沿相同方向飞散；不会再闭合成穿过船身的整圈水幕。持续航行每 3 刻更新一对水花，留下世界坐标泡沫余迹。
- 保留此前确认的高速阈值：水平速度超过 0.48 格/刻开始破浪，低于 0.44 格/刻停止新增。拍击仍独立判定，显著撞水优先于航行水花；第一版的各项总预算不增加。
- **拍水可见度**：水幕贴图保留连续水体，避免噪声遮罩让大片网格透明；顶点不透明度提高，水幕前 40% 生命周期保持完整强度，后半段再淡出。泡沫浪冠由水幕高度的 12% 扩至 30%，提高泡沫连续性与白色层次；撞水水幕适当增加抛起高度和寿命。仍使用环境光、双面半透明绘制及方块遮挡，不改成实心白墙。局部水面查询预算耗尽时保留已确认的水幕基部，避免直接把可见水幕误删。

建议验收：比较少量和多量河豚的半透明气流、尾端停留及松键淡出；正向/倒向高速航行时左右分开的扇形破浪；平拍、船头扎水和侧翻时水幕与白色浪冠的可见度；岸边遮挡。**仅修改代码/贴图并进行静态源码、资源和渲染 API 检查，未编译、未运行测试或启动 Minecraft。**

## 0.7.0：撞水水幕、浪花与高速航行尾迹（第一版）

- **按接触方向拍水**：沿船底、两侧和上沿最多 8×7 个探针取样，接触点位移包含船体俯仰、侧翻与转向。船头先入水、侧舷拍水和船底横拍按各段接触位置产生效果；平拍时横向合并接触点，避免所有水花固定出现在左边。每段移动最多 8 次扫掠取样，降低快速穿过水面时的漏检。
- **动态半透明水幕**：撞击形成随方向、接触速度及取样面积变化的弧形水幕，带薄水层、亮色泡沫浪冠和不规则边缘。水幕在重力下展开、回落并逐渐破碎，浪冠会脱落成水滴。使用代码实时生成网格，适配长船和翻滚姿态，没有使用固定的 Blockbench 水花模型。
- **水滴、水雾与余波**：水滴带惯性和重力，细雾缓慢扩散消失；回落水滴可产生小涟漪。水面泡沫沿环形外扩，留在世界中逐渐消散，不跟着船移动，也不因玩家下船就消失。纹理为 128×128 连续透明度水幕、泡沫、水滴及水雾，颜色混合当地生物群系水色，亮度采用环境光。
- **高速行驶水花**：水平速度超过 **0.48 格/刻（9.6 格/秒）**开始产生迎水面低矮水花及泡沫尾迹，比原版约 0.4 格/刻高约 20%；降到 0.44 格/刻以下停止新增高速尾迹。启停滞回减少阈值闪烁。撞水独立使用接触点向水面运动的速度判定，正常慢速划行和轻微起伏不会持续触发大水花。
- **岸边与性能**：水滴和水幕边缘对方块做射线碰撞；泡沫节点和边中点检查实际水面，遇到干岸后裁掉相应方向。只处理已加载区块中的露天水面，跳过隐藏在碰撞方块中的含水区域。世界切换、传送、首次加载和船体变形不会凭空触发撞水。
- 客户端每刻最多检测附近 24 艘船、提交 20 个事件（单船最多 3 个）、进行 2304 次局部水面查询和 512 次特效碰撞射线。最多保留 16 个水幕、224 个水滴、64 团水雾和 48 个泡沫环；游戏粒子设置为“较少／最少”时进一步降低上限和网格精度。渲染帧只做插值和提交网格，不执行方块碰撞查询。极端船长只取附近有限采样点，不按全部船舱逐一生成特效。

### 后续水面模拟接口

代码位于 `src/main/java/com/xc/longboatlab/client/water/`：

- `WaterImpact`：不可变事件，包含来源船 UUID、世界接触点、相对水流的速度、表面法线、船体法线、取样面积、法向/切向速度、强度及 ENTRY/SKIM 类型。速度单位为格/刻，强度为视觉标量，面积为近似采样面积，不能直接视为排水体积或真实冲量。
- `WaterSurfaceModel.sample`：查询局部水位、单位外法线和水流速度；当前由 `VanillaWaterSurface` 读取原版流体顶面。后续可改为局部高度场，检测和特效仍从同一接口读取水面。
- `acceptImpact` / `tick` / `render` / `clear`：预留注入水面扰动、演算、绘制及切换世界时释放状态的入口。客户端线程调用 `BoatWaterEffects.setSurfaceModel(model)` 替换，`useVanillaSurface()` 恢复。
- `render` 在 Fabric `AFTER_ENTITIES` 阶段调用；按相机相对坐标提交顶点，不跨回调保存 VertexConsumer。接口不会自动隐藏原版水面；以后实现可凹陷水面时还需处理原版水面的遮罩/替换。后续若要使凹陷影响浮力和船体运动，还需要接入服务端物理与同步，当前接口仅覆盖客户端水面和视觉。

**第一版属于局部视觉模拟**：原版水面没有实际凹陷，泡沫不会把附近实体推开；水幕为有限节点的近似碰撞，未实现完整流体、自碰撞或浪花相互作用。多人游戏由各客户端根据收到的船体姿态生成，可见效果可能略有差异。

建议手动验收：单船自由落水；长船船头扎水、船底横拍和侧翻拍水；普通速度划行不连续喷花、加速越过阈值后产生尾迹；沿岸撞水与水滴回落；压缩/展开时不误报；切换维度后无旧特效残留；三档粒子设置及大量船同时运动。**仅完成代码、资源和静态检查，未编译、未运行测试、未启动 Minecraft；最终观感与性能需游戏内验收。**

## 0.6.3：按面增强河豚喷气拖尾

- 五个面的喷流按**该面安装的河豚总数量**独立增大，兼容旧版安装和网格叠放；船底 10 只、船侧 5 只时，底喷比侧喷更长、更粗。
- 基础拖尾目标长度由约 2 格提高到 **4.8 格**；长度倍率为 `1 + 0.30 × log₂(n)`，宽度倍率为 `1 + 0.16 × log₂(n)`。例如 1 / 5 / 10 只分别约为 4.8 / 8.1 / 9.6 格，宽度倍率为 1 / 1.37 / 1.53。数值为稳定喷射时沿喷出方向的目标跨度，实际尾迹也受船体运动、旋转和可见距离影响。
- 增大喷流截面，提高中后段不透明度，贴图改为明亮白芯、青色主体及蓝色外缘，保持自发光，让明亮背景下也更容易辨认。
- 粒子设置为最少／较少／全部时，每只可见河豚使用 6／10／14 段，均覆盖完整目标长度。增加河豚数量只改变各面的尺寸倍率，不额外提高单喷口段数；计数和倍率每次渲染按面统一计算。保留喷口当前帧对齐、历史姿态尾迹和距离裁剪。

按要求仅修改源码与资源并做静态检查，**未编译、未运行测试或启动 Minecraft**。建议在相同距离比较船底 10 只与侧面 5 只同时喷射，并观察白天、夜间及翻滚时的可见度。

## 0.6.2：正常划行、落地稳定与小角度回正

- **正常前进**：普通划桨在地面／水面上沿水平船头方向推进，不再把船身轻微低头转成持续向下的划行动力。巨大桨接地牵引同样保持水平；陆地上开喷气不再额外启用巨大桨的水面划行动力。河豚自身的五面推力仍随船体转向，偏置喷射继续产生翻滚力矩。
- **稳定重心与回正**：船体重心下移，乘客对重量分布的影响适当降低。未喷气、没有快速向上跳离支撑面、且倾斜小于约 65° 时加入带阻尼的回正；削弱粗略接地/浮力采样造成的持续偏转，消除坐在船头附近就慢慢前倾的趋势。接近水平时通过碰撞求解收敛残余小角度。回正仍检查船体、大桨及乘客碰撞，不直接穿过障碍。
- 停喷后接地每刻保留 76%、水中 86% 的俯仰／侧翻角速度，减少长时间摇摆与落地后继续滚动；主动喷射时分别保留 94%／97%，空中保留 99.8%。完全倒置、大幅翻转或腾空时不会强行拉回直立，仍保留翻滚玩法。
- 地面支撑取样只使用船体和巨大桨，乘客继续参与整体碰撞，但不充当用于回正的支撑脚。取样均匀覆盖结构首尾，每刻最多 32 个支撑探针，沿用已有碰撞缓存预算。
- **减少漂浮**：去掉巨大桨接地划动就不断补入 0.05 格/刻上升速度的逻辑，由碰撞支撑船体，保留已有河豚／弹跳的升力。碰撞纠偏抬升改为每刻共享预算：未喷气最多 0.06 格，喷气最多 0.16 格；回正时会在碰撞允许范围内收回本刻多余的抬升距离，避免六个子步各自抬高导致船体飘起。

建议手动验收：普通单节船和长船在平地／静水上持续 W 前进、松键、坐在首尾舱时的稳定；船只倾斜后停喷回正；巨大桨在平地划动是否不再反复起跳；船头底喷与空中翻滚是否仍保留。**本次仅修改代码并做静态源码检查，未编译、运行测试或启动 Minecraft。**


## 0.6.1：HUD 自适应边框、翻滚座位与喷气方向修复

- **HUD 外框**按实际内容高度绘制，包含五面气量行、弹跳行和临时提示。原像素边框拆为顶部、中段和底部，中段平铺延长，保留边角、铆钉和木纹的像素尺寸；窗口缩放依据当前面板总高，气量区域不再覆盖左右边框。
- **翻滚座位**：原版定位是“船的座位锚点 − 乘客的乘坐偏移”。之前只旋转了前者，玩家的 0.6 格偏移仍沿世界竖直方向扣除，翻转时造成明显脱座。现在两个偏移使用相同的船体姿态。船体、座位、相机、玩家渲染和瞄准共用同一帧的位置、伸缩与旋转，消除把乘客直线位置插值和船体旋转插值混用造成的偏移。
- 乘客碰撞和重心使用修正后的身体位置；玩家碰撞采用坐姿包围范围，避免把站立时的腿部区间错误扩展到船底下方。长船头尾座位、压缩伸展、倒置与第一/第三人称继续使用相同座位归属。
- **喷气方向**：五面外法线核对为底 −Y、后 −Z、前 +Z、左 +X、右 −X（均为船体局部坐标）。河豚朝向、嘴部和气流共用这个法线，再随船体转入世界坐标；不根据玩家视角改变喷气面。靠近喷口的两段亮芯紧贴当前帧的嘴部及向外方向，远处尾迹按发射时的姿态移动。
- 气流历史同时保存旋转中心和启用面，避免翻滚/弹跳时混用当前旋转中心，也不在刚启用某面时生成此前不存在的尾迹。气流继承船的平移速度，减少高速时喷口与气流脱节。

按要求仅修改代码并进行源码与 API 签名检查，**未编译、未运行测试或启动 Minecraft**。建议验收 HUD 在有/无提示、不同 GUI 缩放下的完整边框；船头/船尾座位持续翻滚时的乘客与相机位置；五个面单独喷射以及倒置时的喷口方向。


## 0.6.0：重心翻滚、随船乘客与五面网格喷射

- **重力与惯性**：移除按角度硬拉回的回正力，改为根据船体和乘客重心、实际接地支撑范围、船体各段的入水深度计算力矩。重心投影在支撑范围内时可以保持稳定，超出边缘后重力会促使倾倒；水中浮力施加在浸水位置。自由落体时均匀重力只产生下落，不凭空改变朝向。空中转动围绕重心，接地转动围绕支撑位置。
- 降低旋转阻尼，空中每刻保留 99.95%、水中 99.5%、接地 98.5% 的角速度；最大俯仰/侧翻角速度提高到 0.18 弧度/刻。碰撞只消耗受阻方向的角动量，另一个可转动方向继续运动。每刻最多 6 个旋转子步，仍使用上版碰撞缓存与预算；极端尺寸继续限制桨尖扫过的距离。
- **乘客随船翻滚**：乘客模型、第一/第三人称相机的上方和朝向跟随船体倾斜，鼠标仍可相对船体自由观察；交互与钩爪瞄准射线使用相同方向。乘客自身尺寸按船体姿态旋转成碰撞段，加入船只平移、转向、翻滚的整体碰撞检查，头部或身体碰到障碍也能阻止船只继续运动。乘客重量参与重心计算；下船后恢复直立相机和普通碰撞。

### 五个船面的自由网格安装

手持河豚桶瞄准船体，会显示待安装的河豚模型、青色位置框和附近网格；**蹲下右键**安装在当前格子，无需按顺序填入。支持船底、船前、船后、左舷和右舷，河豚脸与喷口始终朝对应面的外侧。船顶不能安装。

- 船底每舱 3×5 格；左右舷每舱各 5×2 格；船前和船后各 3×2 格。当前显示的预览就是服务端安装使用的网格规则。变形期间暂停安装和预览。
- 同格重复安装会叠下一层：前后端面沿竖直方向扩展，保持贴着端面的平面，不向远离船尾的方向伸出；船底和侧面沿外法线增加层。仍无玩法数量上限，仅受 32 位总数量存储范围限制。不同格子互不挪位；重锤拆除所瞄准的一列中的一只，同列后续层收拢，其他格子保持不动。
- 每只新河豚所属的面、船舱和格子随存档、船物品、掉落、创造取船、发射器放置及加长合成保存。压缩时保留原船舱归属，重叠船舱的格子沿外侧分层显示，展开后复原。压缩状态下新安装到底部或侧面的河豚归到中间船舱。
- 旧版按顺序安装的船尾和船底河豚保留旧布局和数量，可以继续拆卸、喷射；新安装使用自由网格。合成长船时侧面/底面格子的船舱编号平移，前后端面的同坐标格子合并叠层。

### 独立喷射按键与蓄气

所有按键可在游戏控制设置中修改，可同时按住多个方向组合喷射，松开哪个就停止哪个：

| 默认按键 | 喷射面 | 反作用方向（相对船体） |
| --- | --- | --- |
| 小键盘 `5` | 船底 | 向上 |
| 小键盘 `2` | 船后 | 向前 |
| 小键盘 `8` | 船前 | 向后 |
| 小键盘 `4` | 左侧 | 向右 |
| 小键盘 `6` | 右侧 | 向左 |
| 左 `Alt` | 所有有河豚的面 | 各面推力与力矩叠加 |

每个面独立储气、消耗、回充。该面有 **n** 只河豚时，满气可喷 **2.5 + (n−1) 秒**；例如 1/5/10 只分别为 2.5/6.5/11.5 秒。耗气速度为回充的两倍，耗尽后要松开该面的喷射键（以及全部喷射键）才能重新蓄气。增加或拆除部件保留气量百分比，不会凭空补满。

底喷升力保持克制；各面推力作用在该面实际安装格子的平均位置，偏置安装产生俯仰、侧翻或水平偏转力矩，翻转后推力随船体改变世界方向。HUD 分别显示五面数量、气量与剩余秒数。只有实际喷射的面鼓起河豚、绘制气流并发声；其余面的气量可以同时恢复。锁钩时沿用球面移动约束，喷射作为球面机动加速，不解除锁定。

### 新的网格调试参数

`/longboatlab rig spawn/give` 在 0.5.0 参数基础上新增 `Grid` 列表：

```mcfunction
/longboatlab rig spawn {Length:4,LeftCount:0,RightCount:0,Grid:[{Face:"bottom",Slot:4,U:0,V:2,Count:6},{Face:"stern",U:0,V:0,Count:3},{Face:"left",Slot:2,U:1,V:1,Count:2}],Distance:8d,Height:1d}
```

| Grid 字段 | 含义 |
| --- | --- |
| `Face` | 必填，`bottom/stern/bow/left/right` |
| `Slot` | 从 1 起的船舱编号；默认 1。底和侧面可选任意船舱；前后端面使用 1 |
| `U` | 默认 0。底/前/后横向 −1～1，正值偏左；左右侧面纵向 −2～2，正值偏船头 |
| `V` | 默认 0。底面纵向 −2～2，正值偏船头；其余面为 0/1 两个高度 |
| `Count` | 该网格列叠放数量，默认 1，正整数；所有旧布局与网格河豚合计不超过 2,147,483,647 |

同一网格坐标不能在列表里重复；`Grid` 可以和 `SternPuffers`、`Bottom` 同时使用，后两者仍生成兼容旧版的顺序布局。

本版仍是分段包围盒与有限采样的简化刚体物理，未引入精确网格碰撞或完整流体模拟。建议重点验收：船首单独底喷后松键的惯性、岸边单端支撑倾倒、五面不同格子放置/拆除/折叠保存、组合喷射与独立气量、乘客倒置时头部碰墙/天花板、第一/第三人称瞄准一致性及多人同步。**仅修改代码并检查源码、资源和 API 签名，未编译、未运行测试、未启动 Minecraft。**


## 0.5.0：巨大桨碰撞优化、抓地与物理翻滚

- **TPS 原因（源码定位，未实测剖析）**：旧巨大桨每刻的扫掠采样次数随尺寸增大，每次采样又逐段重新查询方块和实体碰撞。现改为每根桨固定 12 个碰撞段、每刻 4 个关节子步，受阻时最多追加 3 次二分；曲线包络覆盖子步之间的路径。同艘船的关节、船体平移与翻滚共享当刻的分块碰撞缓存，避免重复扫描同一区域。
- **抓地**：移除“受阻位移 × 尺寸增益”的弹射冲量。桨叶接地后用阻尼摩擦追踪目标速度，单刻水平速度修正最多 0.12 格；下压支撑的向上速度目标最多 0.05 格/刻。墙面阻挡关节，不会继续累积弹飞力量。无阻挡的巨大桨仍按普通桨的 π/8 弧度/刻相位速度划动。
- **降低底喷升力**：每刻总底喷速度增量为 `min(0.10, 0.018 × sqrt(船底河豚数) / sqrt(1 + 0.30 × (船长节数 − 1)))`，方向沿船体自身上方。数量有递减收益，长船更重；船尾推进与按住喷气、松键回充规则继续保留。
- **物理翻滚**：服务端保存俯仰、侧翻角度和角速度。船底喷口按船舱纵向位置产生俯仰力矩，未对称填满的底部预设点产生侧翻力矩；仅船头底部喷气会抬起船头，并可在有空间时继续向后翻转。力矩考虑船长惯量，转角可超过 90°、180°。船体和巨大桨的实际碰撞段随之旋转，触地消耗角动量；有小幅位置纠偏以绕接地点转动，不额外注入弹射速度。倾斜后的浮力采样跟随船体，而非仍使用直立船的吃水逻辑。
- 渲染插值同步船体、船桨、箱子、河豚及粒子；乘客座位随船体移动，并检查乘客头部空间，玩家自身保持直立。实体存档保留角度和角速度，打掉后的船物品不保存空中姿态。锁钩时暂停翻滚以保持已有球面约束；解锁后恢复物理计算，下船不重置已有旋转。
- **下船避障**：先寻找当前船舱两侧，再搜索附近落脚点，检查站立/蹲下空间，并明确排除船体和巨大桨碰撞段。没有地面落脚点时可以选择空中的无碰撞位置。完全封闭且无可用位置时仍回退原版选择。

### 一键生成详细改装船

需要管理员权限（等级 2），使用 SNBT 配置，可省略不需要修改的字段：

```mcfunction
/longboatlab rig spawn {Length:4,Wood:"oak",LeftCount:4,RightCount:4,LeftSize:4d,RightSize:4d,SternPuffers:2,Bottom:[{Slot:4,Count:6}],Distance:8d,Height:2d}
/longboatlab rig give {Length:4,Left:[{Slot:1,Size:3d},{Slot:4,Size:5d}],Right:[{Slot:1,Size:3d},{Slot:4,Size:5d}],Bottom:[{Slot:4,Count:9}]}
```

`spawn` 直接生成实体，放置空间包含巨大桨；`give` 给出对应船物品。列表内可逐根指定不同大小的巨大桨，左右独立。已有普通桨船指令及 `/longboatlab oar <倍数> [数量]` 继续可用。

| 字段 | 默认值 | 含义与范围 |
| --- | --- | --- |
| `Length` | `1` | 船长节数，1～2,147,483,647；指令可超过正常合成长限制 |
| `Wood` | `"oak"` | `oak/spruce/birch/jungle/acacia/dark_oak/mangrove/cherry/bamboo` |
| `Chest` | `false` | 是否箱子船，`true/false` |
| `LeftCount`、`RightCount` | 各 `1` | 左/右巨大桨数，0～min(船长, 512)，沿船舱均匀布置；一根时放在中间舱 |
| `LeftSize`、`RightSize` | 各 `4` | 对应侧统一尺寸倍数，1.001～约 1290.159，支持小数 |
| `Left`、`Right` | 无 | `[{Slot:1,Size:4d},…]`；覆盖该侧 Count/Size，每侧最多 512 根，同侧船舱不能重复 |
| `SternPuffers` | `0` | 船尾河豚数，非负整数 |
| `Bottom` | `[]` | `[{Slot:4,Count:9},…]`；指定各船舱底部数量，船舱不能重复 |
| `Yaw` | 执行者朝向 | 水平朝向，−360～360 度 |
| `Pitch`、`Roll` | 各 `0` | 初始抬头/侧翻角，−360～360 度，仅 `spawn` 支持非零角度 |
| `Distance` | `4` | 从执行位置沿执行者水平朝向前移，0～256 格，`spawn` 使用 |
| `Height` | `0` | 相对执行位置上下偏移，−64～256 格，`spawn` 使用 |

`Slot` 从 **1** 开始，**1 = 船尾舱，Length = 船头舱**；左右以面向船头为准。所有船尾与船底河豚之和最多为存档整数范围 2,147,483,647，不新增玩法数量限制。巨大桨逐根存组件，因此详细指令每侧最多分配 512 根；它仍遵守每舱每侧一个巨大桨位、装大桨不能折叠的规则。字段拼写或数值错误会拒绝执行。

直接观察船头底喷翻滚，可在开阔水面使用以下无桨示例，登船后按住喷气：

```mcfunction
/longboatlab rig spawn {Length:4,LeftCount:0,RightCount:0,Bottom:[{Slot:4,Count:9}],Distance:8d,Height:1d}
```

### 性能边界与手动验收

碰撞采用旋转后的小段轴对齐包围盒，属于简化刚体物理；斜向贴边有保守余量，不是完整流体或精确网格模拟。每船每刻最多加载 256 个 4×4×4 碰撞缓存块，单次查询最多覆盖 64 块；超出预算或碰到未加载区块时阻止相应动作，不会跳过碰撞。极端尺寸的指令船可能无法生成或划动；固定子步消除了采样随尺寸增长，但很多船或很多巨大桨仍有计算成本。

骑船执行 `/longboatlab physics` 可查看最近一刻 `tiles`（地形缓存块数）、`queries`（几何查询次数）、`deferred`（因预算/未加载区域拒绝的查询数）。这不是耗时或 TPS 测量器；游戏内还需对照服务器实际表现。

建议分别检查 2/4/8/16 倍巨大桨在平地、墙边、水边的划动和抓地；检查船头单独底喷、均匀底喷、停喷及落地时的翻滚；检查长船头尾、大桨旁和低顶环境下的下船；检查存档重载、多人观察、钩爪锁定与解锁。**本次仅修改源码并做静态检查，未编译、运行测试或启动 Minecraft。**


## 0.4.1：快速变形、按住喷气与船底河豚

- 巨大桨与普通桨使用相同的 **π/8 弧度/刻**划动相位速度及摆动幅度，不再因尺寸变大而降低角速度。撞到地形时仍按实体碰撞限制实际关节动作。地面接触反作用力增强，按桨尺寸提高抓地作用；多根接地桨共同出力。水中巨大桨按桨面尺寸计入推进力，不再只算普通一根桨。
- 所有船的 **GUI 物品模型统一等比缩小 50%**，包括普通船、箱子船、长船及带巨大桨的船。不会按船长额外缩小，长船仍可溢出格子；手持和地面实体模型尺寸不受 GUI 缩放影响。
- 管理员新增 `/longboatlab oar <尺寸倍数> [数量]`：例如 `/longboatlab oar 4 2` 获得两根 4 倍巨大桨，`/longboatlab oar 2.5` 获得一根 2.5 倍巨大桨。数量默认 1，单次可给 1～64 根；倍数支持小数，范围 1.001～约 1290.159（材料量仍用 32 位整数保存）。精确倍数随物品、安装、拆卸、存档及船体掉落保留；继续参与合成时按累计材料体积重新计算尺寸。
- **按住左 Alt（可改键）喷气，松开立即停止并回充**。HUD 氮气行改为气量条及百分比：100 点满气，每刻喷气消耗 2 点，松键每刻恢复 1 点，因此满气可喷约 2.5 秒，从空恢复约 5 秒。耗尽后必须松键才能重新蓄气，避免一直按住时断续抖动喷射。下船、打开菜单、失焦或输入失联会停止喷射。
- 喷气气流扩大、加亮，增加尾迹密度及喷口亮芯，采用自发光材质，在水下与暗处也能看清。源点仍逐帧绑定河豚嘴部。循环声只在实际喷射期间播放，位置选择靠近听者的船尾或船底河豚。
- 形态切换缩短为 **2 刻（约 0.1 秒）**，保留极短的伸缩过渡，接近瞬间完成；不再使用原来的 0.8 秒拖长动画。
- 船尾河豚现在可以 **蹲下持重锤对准河豚右键拆下**，返还可再次安装的河豚桶；船底河豚也使用相同操作。拆卸后该组余下河豚向中心收拢填补空位。
- **蹲下持河豚桶，必须对准船舱底面右键**才能安装朝下的河豚。展开船可指定任意船舱；每舱以中心开始，向外按长方形环顺序铺满 3×5 个预设点。取消所有玩法数量上限，满一层后沿底部向下增加下一层；船尾则继续向上增加横排。
- 船尾河豚向后喷气推进；船底河豚向下喷气，给船提供向上的反作用力。两处同时安装时，按各自数量合并前向推力与升力；松键同时停止所有喷口。锁钩时沿用球面机动加速，仍保持固定链长约束。
- 船底数据按 **船舱编号＋该舱数量**独立保存。折叠时集中到压缩船底显示，展开后恢复所属船舱，合成加长会平移船舱编号；打掉再放、创造取船、发射器放置和实体存档都保留。旧存档里的河豚自动视为船尾河豚。船物品说明显示船尾/底部分项数量。
- 普通手动安装与加长合成都移除了 8 只河豚限制，只保留存档整数溢出检查。世界中的河豚与喷气没有固定数量绘制上限，按距离查询附近排/层；GUI 中极密集的河豚仍做抽样以控制物品预览开销。河豚不增加船体移动碰撞。


## 0.4.0：巨大桨步行船、完整物品模型与伸缩动画

- 船物品在**主手、副手、第三人称、掉落状态、展示框、物品栏及快捷栏**中共用实际船体模型。物品栏保持固定正交角度，按真实长度绘制，**不再缩小到格子里**；长船会夸张地越出物品格。普通船、改装船和巨大桨船均使用这条渲染路径。
- 河豚喷口改为逐帧绘制的粒子面片，源点直接使用同一帧的船体位置、船尾长度和河豚嘴部坐标；后方气流使用短时间位置记录形成尾迹，避免粒子排队延后一刻造成喷口脱节。保留按距离剔除，粒子设置只降低每个喷口的尾迹密度，不限制河豚数量。
- 新增独立的无缝循环喷气音效，持续到 40 刻喷射结束。空间音源跟随靠近听者高度的船尾河豚嘴部；大量河豚合用一个随动音源，数量影响响度，避免成千上万个重叠声音。下船后仍跟随正在喷气的船，喷射结束、船消失或切换世界时停止。
- 长船与压缩形态采用 **16 刻（0.8 秒）**的平滑伸缩；船底和侧板延伸、首尾平移、隔断渐渐展开，叠层高度与普通桨位置同步过渡，座位也沿过渡位置移动。动画期间保留展开长度的碰撞空间，避免收缩中让障碍穿过仍可见的船身；动画结束前不能重复切换、弹跳、发钩、安装巨大桨或新上船。
- **巨大桨合成：任意放入 3～9 把木铲，每格一把，得到一根巨大船桨。**背包 2×2 合成也可放 3～4 把。普通木铲算 1 份材料，巨大桨保留原有材料量，继续合成时相加；长度、宽度、厚度按材料量的立方根等比放大，例如 8 份材料为 2 倍尺寸。输出保留第一格木铲的名称、附魔、耐久等组件，说明显示材料量和尺寸。两把普通木铲的原版修复配方不变；含巨大桨的原版修复配方被拦截，避免修复时丢失尺寸。
- **先用重锤拆空普通桨，再蹲下持巨大桨右键船舷安装。**普通桨与巨大桨不能混装，不同尺寸的巨大桨可以共存。每节船体预设左、右各一个巨大桨位，按瞄准位置选择最近的预设点；该位置已占用时拒绝安装。重锤拆下时返还对应尺寸和组件的木铲。
- 给压缩船安装巨大桨会先验证展开船体和巨大桨所需空间，通过后自动展开；不通过则恢复原状且不消耗物品。**船上还有任何巨大桨时都不能折叠**，打掉、再次放置、发射器放置和继续加长合成后也保持展开；空间检查包含巨大桨。巨大桨越大，需要的侧向与下方空间也越大。
- 巨大桨使用与可见关节姿态一致的分段实体碰撞，普通桨仍不加入船体移动碰撞。W/S 划动巨大桨时，触地方向的运动产生抬升与摩擦推进，碰到障碍时产生反作用力；足够长的桨可以支撑、撑起船体并协助陆地移动。陆地上关闭普通水面划行动力，由触地桨驱动，河豚加速仍可用。每侧独立提交无碰撞的划动范围；碰墙会停住关节，转向也检查突出船身的巨大桨。
- 巨大桨尺寸、安装位置随船物品和实体保存，实体读档恢复关节姿态，避免重载时突然把正在支撑船体的桨收回。碰撞缓存按位置、朝向、安装数据和关节角度失效，普通大数量调试船仍使用紧凑桨计数。

下面的旧版本记录用于说明已有功能；此节描述 **0.4.0** 发布时的行为；最新变化见顶部 **0.8.0**。

## 0.3.1：实物图标、移动实体钩爪与性能补丁

- 加长船在物品栏、快捷栏与容器中使用实际船体模型的正交视角缩略图，按木材、真实船长、左右桨和河豚显示；默认折叠的物品也展示展开外观，以便辨认实际长度。0.4.0 起保留模型实际大小，不再缩入物品格。
- 河豚氮气统一持续 **40 刻（2 秒）**，结束后冷却 100 刻。数量增加推进力，不再延长持续时间。取消每船 8 个喷气点和 64 只河豚的固定绘制上限；所有处在可视距离内的河豚均可显示和喷气，远处仍按距离剔除，并服从游戏粒子设置。
- 压缩形态的划行推进按每侧最多 1 根、合计最多 1 对桨计算；展开才使用实际全部桨数。左右数量不平衡导致的偏航仍保留。取消固定划行航速封顶，超过 64 根的桨仍按真实数量提高推进力。
- 正常安装与合成的容量改为 **每节每侧 16 根，即每节 16 对**；2 节每侧 32 根、16 节每侧 256 根。调试命令生成的数量不受这个容量限制。容量与模型抽样数量独立，实际推进力不使用模型抽样后的数量。
- 钩爪从**船头舷缘**发射，发射速度 **4.5 格/刻**；收回基础速度 **5.5 格/刻**。客户端在相邻钩头位置之间逐帧插值，实体锚点按目标实体插值位置与朝向同步显示。
- 可以勾住方块或实体，包括生物、玩家、车辆、掉落物等具有包围盒的实体；不勾住发射者、当前船及其乘客、旁观实体。命中检测比较完整飞行段内的方块与实体，不会隔墙抓目标。锚点保留命中位置相对目标的偏移，随目标平移和转向；船随移动锚点保持当前链长。
- 驾驶员下船后**已经锁定**的钩爪保持锁定，空船继续悬挂或跟随目标。重新上船后可继续操作。未命中的飞行钩爪仍在离船后收回。目标消失、跨维度、锚块变化或刚性连接受到障碍阻挡时解除；技能仍是临时状态，卸载实体后重置。
- 球面移动速度提高到 **0.9 格/刻**（氮气期间 1.8）；W/S 收放速度 **0.8 格/刻**（氮气期间 1.5）。方向键仍保持当前长度为半径，碰撞不会把船推入球内；移动锚点被墙挡住时解除连接，避免强行穿墙。
- 新增 **H：显示／隐藏船只 HUD**，可在原有“长船实验室”按键分类中改键。开关影响仪表盘和模组提示，不影响操控。
- 性能调整：船只空间索引替代每次碰撞时全量扫描；同一姿态复用分段碰撞体；轴对齐船体合并等价碰撞段；无移动、无转向时跳过多余查询。水面与地面环境检测沿船身进行，避免扫描斜向包围盒的空白区域；超过 128 节的调试船使用覆盖首尾的分布式浮力采样。压缩叠层按可见高度绘制，模型和物品预览缓存各有容量限制。


## 0.3.0：默认压缩、船位乘坐、像素 HUD 和调试船

- 加长合成产物默认压缩。所有超过 1 节的船物品（包括旧版本物品、拆下的船及发射器放置）都以压缩形态放下，再按 R 展开。世界内实体存档仍保留当前形态，重进世界不会自动折叠已展开的船。静态压缩叠层的额外高度最多 0.35 格。
- 河豚转向船尾外侧。氮气粒子改为客户端按船尾河豚安装位置生成，与客户端船体插值共用时间线，避免单节高速船的粒子出现在船头。
- 修复原版附近实体检索遗漏长船首尾的问题：客户端与服务端维护已加载船列表，按整个船身检索；其他实体接近长船时也使用船身分段碰撞，而非只在船中心附近检测。16 节船覆盖约 32 格的完整船长，仅保留船头船尾各 0.10 格的上层碰撞余量。
- 对准哪一节船舱上船，就优先坐在那节的前后位置；附近已有乘客时选择空位。座位按乘客 UUID 同步并保存，第二位乘客上船不会挪动第一位乘客。压缩时将乘客收回单节船内，展开后恢复所选长船座位。载客上限仍为普通船 2 位、箱子船 1 位。
- 屏幕左上角新增贴图式像素仪表盘：船形态、节数、左右桨数、河豚数、左右桨比例、钩爪状态／当前长度、氮气和弹跳就绪进度。驾驶或瞄准船时显示，F1 时隐藏，随 GUI 缩放；改装结果、上限及空间不足提示显示在面板下方，不占用 actionbar。
- 新增管理员调试命令，允许数量超过正常改装上限；普通桨使用“计数 + 特殊木铲组件”的紧凑存储，大量普通桨无需逐个保存物品。兼容读取旧版的木铲列表。

调试命令需要 **2 级权限**（单人开启作弊或服务器管理员）：

```mcfunction
/longboatlab spawn <船长节数> <左桨数> <右桨数> <河豚数> [木材]
/longboatlab give <船长节数> <左桨数> <右桨数> <河豚数> [木材]
```

例如：

```mcfunction
/longboatlab spawn 16 64 64 8
/longboatlab give 128 10000 8000 100 spruce
```

`spawn` 在执行位置沿朝向前方 2 格生成压缩船，空间不足时报告失败；`give` 将成品船物品交给执行玩家，背包满时掉在身边。省略木材时为 `oak`，木材名称支持 Tab 补全。船长最小为 1，其余数量最小为 0；参数支持到有符号 32 位整数上限 **2,147,483,647**，不受正常玩法的船长、每节桨容量和河豚数量限制。

极大调试船的实际数量随存档、拆桨、掉落和再次放置保留；HUD 对大数使用 k/M/B 缩写，物品说明显示完整数量。为控制绘制开销，最多绘制每侧 256 根可见桨、128 个压缩叠层和 512 条隔断；河豚与喷气按距离剔除，不设固定数量上限，船身总长度仍按实际值延伸；碰撞分段最多每层 2048 段，始终覆盖完整长度。通常玩法的数量不会触发这些抽样。参数范围不等于所有极端船长都适合实时模拟，展开后的空间查询仍受世界规模、已加载区块和机器性能限制。

## 0.2.1：隔断、船桨钩爪、操控与碰撞修正

- 舱内隔断横板统一为 **n−1 条**：1 份船长没有横板，2 份为 1 条，3 份为 2 条，以此类推；压缩形态每层也不再有多余横板。乘客座位数量仍沿用原版。
- 静水目标吃水深度改为 **0.18 格**，露出大部分船舷。船桨支点移到舷缘上方，增加向船内伸出的握柄，桨杆搭在船边。
- **R**：驾驶员切换长船 / 压缩形态。压缩形态占用普通船长度，将实际份数变成收拢的竖向叠层；切换回长船会恢复实际长度、全部船桨和相应碰撞。空间不足时不能展开。
- **空格（游戏跳跃键）**：压缩形态在地面或水面发动弹簧跳跃，叠层展开后回缩。初始起跳速度按份数递增，跳跃冷却 35 刻，带本次弹跳的落地保护。锁钩、发射、收回过程中按跳跃键优先解除钩爪，不会同时弹跳。
- **G**：压缩形态沿驾驶员视线发射船桨拼成的三爪钩头，再按收回。每侧外观只保留一根桨，实际桨数不变；无桨时不能发射。最大船桨链长度为 `min(192, 6 + 总桨数 × 2.5)` 格。
- 钩头逐段飞行、对方块做连续命中检测，接触碰撞面后锁定。射空到达最大长度、主动解除、锚块改变、未命中时驾驶员离船或船桨链被新增障碍挡住时，播放收回动画。不会因发射钩爪而强行加载远处区块。
- 锁钩后无重力悬挂：**W/S** 收短 / 放长船桨链；**↑↓←→** 在以锚点为球心、**当前链长为半径**的球面上移动。链长最短 2 格，最长为本次发射时的长度上限，收放链与半径同步提交。不按 W/S 时严格保持半径；遇到障碍会停止本次移动，不会滑入球内或改写半径。环绕方向随球面连续变化，越过上下极点时不会突然反向。
- 从船体到钩头的整段连接均由同材质的船桨首尾重叠拼成，保持笔直刚性，没有金属钢索、下垂或摆动。发射与收回时逐节伸缩，显示长度直接跟随船体与钩头的位置。
- 各种船均可**蹲下，手持河豚桶，右键船尾**挂河豚，最多 **8 只**，生存模式返还水桶。河豚紧贴船尾，横向每排最多 4 只，第 5～8 只向上排第二排，所有鱼保持在同一个船尾平面；取消吊索和悬浮晃动。**左 Alt** 发动氮气，河豚鼓起，船尾喷出烟雾、气泡和亮点，驾驶员视野快速扩张后平滑回落。
- 河豚越多，加速越强，持续时间固定为 `40` 刻，每次结束后再冷却 100 刻。锁钩时加速增强收放线与球面移动速度；无船桨也可以使用已安装的河豚加速。
- 所有新增按键可在 **选项 → 控制 → 按键绑定 → 长船实验室** 中修改。WASD、跳跃跟随游戏原有绑定。仅驾驶员能控制技能；聊天、菜单或窗口失焦时不发送有效操作。
- 河豚数与船桨随实体存档、掉落、再次放置及发射器放置保留；继续合成会合并河豚数，超过正常上限不出结果，合成产物使用压缩形态。世界内实体保留形态，长船物品默认压缩放置。钩爪锚点、发射状态、弹跳和加速计时是临时状态，重新加载实体时重置，避免读档后被旧锚点拉走。
- 压缩形态用重锤拆可见桨时，每次取回该侧最后安装的一把木铲；该侧实际桨数降为零时才隐藏该侧桨。
- 船只移动改为服务端计算，客户端发送按键并插值显示服务端位置，避免钩爪锁定时两端争夺船坐标。多人环境下操作响应会受网络延迟影响。

## 原有加长与改桨

- 原版九种材质的船、竹筏及对应箱子船，全部使用新的船桨与动力逻辑；已有存档里的船默认左右各一根桨。
- 把 **2～9 艘同材质、无箱子的船**放入工作台任意格子即可合并。普通船算一份船长，已有加长船按实际船长计入；可以反复合成，左右桨数也分别相加。输出仍是带数据的原版船物品，悬浮说明显示船长和左右桨数。背包合成格也支持放得下的同一配方。
- 蹲下、手持**木铲**、右键船的左侧或右侧，消耗一把木铲，在这一侧添加一根可见船桨。左右以面向船头为准，不按世界东、西区分。船头、船尾、船底不能加桨。
- 蹲下、手持**重锤**、瞄准某根船桨右键，拆下该根桨。返还对应木铲，保留耐久、附魔、名称等物品组件。背包满时掉到玩家身边；生存模式重锤损耗一点耐久。
- 两侧桨数相等时，采用原版转向加速度和环境阻尼，总桨数不放大转向；前进以原版一对桨的推进力为基准，按 `sqrt(总桨数 / 2)` 增长，取消长船额外的起步惩罚。数量不同才会向桨较少的一侧偏航，偏航按比例平滑增加并限制最高角速度。推进时两侧达到 **4:1** 或一侧为零，逐渐消除平移惯性并进入原地打转。两侧全部拆空后，W/A/S/D 不再提供推进与转向力；重力、浮力、外力和已有惯性仍保留。
- 长船使用一体船底、两块连续长侧板和唯一的一组首尾封板。船体并非把完整原版船模型首尾堆叠，不会出现中段重复端墙、船体间隙或断开的连接处。
- 支持实体存档、掉落再放置、发射器放置，以及服务端同步桨数。箱子船仍可载货，普通船仍为两个座位，箱子船一个座位。
